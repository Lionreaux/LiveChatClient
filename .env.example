# Configuration pour le client Electron
# Copiez ce fichier en .env et remplacez par vos vraies URLs

# URL du backend (Render, Railway, ou localhost)
BACKEND_URL=https://votre-backend.onrender.com

# URL WebSocket (même que le backend)
WS_URL=https://votre-backend.onrender.com

# Pour le développement local :
# BACKEND_URL=http://localhost:3000
# WS_URL=http://localhost:3000
